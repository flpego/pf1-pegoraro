import { FormsValidatorsPipe } from './forms-validators.pipe';

describe('FormsValidatorsPipe', () => {
  it('create an instance', () => {
    const pipe = new FormsValidatorsPipe();
    expect(pipe).toBeTruthy();
  });
});
